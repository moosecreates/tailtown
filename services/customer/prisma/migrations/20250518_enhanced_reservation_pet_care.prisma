// This is a migration file for Prisma Migrate

-- CreateEnum
CREATE TYPE "PetFeedingTime" AS ENUM ('MORNING', 'LUNCH', 'EVENING', 'SNACK');  

-- CreateEnum
CREATE TYPE "MedicationFrequency" AS ENUM ('ONCE_DAILY', 'TWICE_DAILY', 'THREE_TIMES_DAILY', 'AS_NEEDED');

-- CreateEnum
CREATE TYPE "MedicationTiming" AS ENUM ('MORNING', 'AFTERNOON', 'EVENING', 'BEDTIME');

-- CreateEnum
CREATE TYPE "RecurrenceFrequency" AS ENUM ('DAILY', 'WEEKLY', 'MONTHLY');

-- CreateEnum
CREATE TYPE "LodgingPreference" AS ENUM ('STANDARD', 'SHARED_WITH_SIBLING', 'SEPARATE_ACCOMMODATIONS');

-- AlterTable
ALTER TABLE "reservations" ADD COLUMN "lodgingPreference" "LodgingPreference" DEFAULT 'STANDARD';

-- AlterTable
ALTER TABLE "pets" ADD COLUMN "allergies" TEXT,
ADD COLUMN "medicationNotes" TEXT;

-- CreateTable
CREATE TABLE "pet_feeding_preferences" (
    "id" TEXT NOT NULL,
    "petId" TEXT NOT NULL,
    "reservationId" TEXT,
    "feedingSchedule" "PetFeedingTime"[],
    "foodType" TEXT,
    "foodAmount" TEXT,
    "specialInstructions" TEXT,
    "foodAddIns" TEXT[],
    "probioticNeeded" BOOLEAN NOT NULL DEFAULT false,
    "probioticDetails" JSONB,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "pet_feeding_preferences_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "pet_medications" (
    "id" TEXT NOT NULL,
    "petId" TEXT NOT NULL,
    "reservationId" TEXT,
    "name" TEXT NOT NULL,
    "dosage" TEXT NOT NULL,
    "frequency" "MedicationFrequency" NOT NULL,
    "timingSchedule" "MedicationTiming"[],
    "administrationMethod" TEXT,
    "specialInstructions" TEXT,
    "startDate" TIMESTAMP(3),
    "endDate" TIMESTAMP(3),
    "isActive" BOOLEAN NOT NULL DEFAULT true,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "pet_medications_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "recurring_reservation_patterns" (
    "id" TEXT NOT NULL,
    "reservationId" TEXT NOT NULL,
    "frequency" "RecurrenceFrequency" NOT NULL,
    "daysOfWeek" INTEGER[],
    "interval" INTEGER NOT NULL DEFAULT 1,
    "endDate" TIMESTAMP(3),
    "occurrenceLimit" INTEGER,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "recurring_reservation_patterns_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "pet_feeding_preferences_pet_idx" ON "pet_feeding_preferences"("petId");

-- CreateIndex
CREATE INDEX "pet_feeding_preferences_reservation_idx" ON "pet_feeding_preferences"("reservationId");

-- CreateIndex
CREATE INDEX "pet_medications_pet_idx" ON "pet_medications"("petId");

-- CreateIndex
CREATE INDEX "pet_medications_reservation_idx" ON "pet_medications"("reservationId");

-- CreateIndex
CREATE UNIQUE INDEX "recurring_reservation_patterns_reservationId_key" ON "recurring_reservation_patterns"("reservationId");

-- AddForeignKey
ALTER TABLE "pet_feeding_preferences" ADD CONSTRAINT "pet_feeding_preferences_petId_fkey" FOREIGN KEY ("petId") REFERENCES "pets"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "pet_feeding_preferences" ADD CONSTRAINT "pet_feeding_preferences_reservationId_fkey" FOREIGN KEY ("reservationId") REFERENCES "reservations"("id") ON DELETE SET NULL ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "pet_medications" ADD CONSTRAINT "pet_medications_petId_fkey" FOREIGN KEY ("petId") REFERENCES "pets"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "pet_medications" ADD CONSTRAINT "pet_medications_reservationId_fkey" FOREIGN KEY ("reservationId") REFERENCES "reservations"("id") ON DELETE SET NULL ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "recurring_reservation_patterns" ADD CONSTRAINT "recurring_reservation_patterns_reservationId_fkey" FOREIGN KEY ("reservationId") REFERENCES "reservations"("id") ON DELETE CASCADE ON UPDATE CASCADE;
