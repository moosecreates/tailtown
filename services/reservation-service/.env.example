# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5433/reservation?schema=public"
DATABASE_SSL_ENABLED=false # Set to true in production
DATABASE_MAX_CONNECTIONS=10
DATABASE_IDLE_TIMEOUT=10000

# Server Configuration
PORT=4003
NODE_ENV=development # development, test, production
HOST=localhost
TRUST_PROXY=false # Set to true if behind reverse proxy

# Security Configuration
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h
JWT_REFRESH_TOKEN_EXPIRES_IN=7d
PASSWORD_SALT_ROUNDS=12
SESSION_SECRET=your_session_secret_here

# API Integration - Microservice Communication (IMPLEMENTED Nov 7, 2025)
# HTTP-based service-to-service communication with retry logic
CUSTOMER_SERVICE_URL=http://localhost:4004
SERVICE_TIMEOUT=5000 # Request timeout in milliseconds
SERVICE_MAX_RETRIES=3 # Number of retry attempts for failed requests (1s, 2s, 4s)
SERVICE_RETRY_DELAY_MS=1000 # Base delay between retries (exponential backoff)
